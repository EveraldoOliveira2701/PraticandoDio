const debito = -0.1;
const dinPix = -0.15;
const parcVezes = 0.1;
const valorParcAvista = 100;
const pagamento = dinPix
if (pagamento == debito) {
    const pagamento1 = (valorParcAvista * pagamento) + valorParcAvista;
console.log(pagamento1) ;   
} else if (pagamento == dinPix){
    const pagamento1 = (valorParcAvista * pagamento) + valorParcAvista;
    console.log(pagamento1);
}else if (pagamento == parcVezes){
    const pagamento1 = (valorParcAvista * pagamento) + valorParcAvista;
    console.log(pagamento1);
}else if (pagamento == valorParcAvista){
       
        console.log(pagamento)
}